<div class="ef-field" data-type="{{ attributes.type }}" data-name="{{ attributes.name }}">
    {% include "./partials/label.njk" %}
    {% include "./partials/description.njk" %}
    <input
        {% for key, value in attributes %}
        {{ key }}="{{ value }}" {% endfor %}
        class="{{ html.class | join(' ') }}"
        >
</div>